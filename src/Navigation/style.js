import { StyleSheet, Dimensions } from "react-native";

let styles = StyleSheet.create({
  tabBarStyle: {
    paddingTop: 20,
    height: 70,
    paddingBottom: 12,
    paddingTop: 12,
    shadowColor: "#000",
    shadowOffset: {
      width: 0,
      height: 12,
    },
    shadowOpacity: 0.6,
    shadowRadius: 16.0,
    elevation: 24,
  },
});

export default styles;
